+++
title = "CS241 coursework - intrusion detection system in C"
description = "A libpcap-based multithreaded packet sniffer to detect SYN, ARP, and HTTP requests."
[extra]
status = ""
languages = "C"
technologies = "pthreads"
+++
The system records the number of SYN packets, ARP packets, and HTTP requests that come from or go to one of the blacklisted URLs (`www.google.co.uk`, and `www.facebook.com`).

A listener thread waits for packets, and copies them to a job queue, which is read from by some worker threads.
